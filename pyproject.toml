[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "software-engineer"
version = "0.1.0"
description = "Minimal scaffold for SOFTWARE-ENGINEER"
authors = [
  {name = "Oratile Masilo"}
]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
